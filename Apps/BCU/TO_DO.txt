+-----------+
| ANOTAÇÕES |
+-----------+

- incluir arquivos do BRisa (OK)
- criar uma classe baseada em um control point (OK)
- modificações no control point original (em progresso):
  * o device encontrado deve possuir os serviços que possuam as ações abaixo,
    caso contrário, o device não será válido (ver OBS1)
    ~> getListOfApps
    ~> getAppInfo
    ~> getApp
    (OK, apesar de não ter nenhum teste ainda, já que isto requer um device)
  * o control point deve ser capaz de decodificação a compactação .compe
    (conversar com o Bruno sobre essa compactação)
- realizar a inicialização de todos os serviços necessários a um cp (ver OBS2)
- criar uma interface gráfica; inicialmente, essa interface pode ser baseada no
  BRisaControlPoint a fim de testar funcionalidades específicas do código. A
  ideia é que posteriormente seja feita uma interface nova para o BCU.

OBS.:
1. o provável método relativo aos serviços que será modificado no novo cp será
   replyFinished em ControlPoint (no BRisa, em upnp/controlpoint)
2. o BRisaControlPoint (na pasta examples do repositório) deve servir de base
